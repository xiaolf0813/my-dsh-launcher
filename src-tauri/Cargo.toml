[package]
name = "dsh-launcher"
version = "0.3.0"
description = "Frameless Windows shell around the DeepSeek Harness web UI"
edition = "2021"

[lib]
name = "dsh_launcher_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6", features = [] }

[dependencies]
tauri = { version = "2.11", features = [] }
serde_json = "1"
